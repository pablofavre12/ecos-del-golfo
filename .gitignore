# Base de datos local (se regenera con importar.py)
ecos.db

# Fixtures sintéticos (se regeneran con generar_fixtures.py)
fixtures/

# Datos reales — NUNCA al repo (los crudos viven en discos, D7)
datos/
crudos/
*.WAV
# …pero los clips SINTÉTICOS de la vitrina sí se publican (en macOS git
# ignora mayúsculas/minúsculas y *.WAV pisa también los .wav de docs/)
!docs/media/clips/*.wav

# Respaldo del catálogo (R20): las copias .db quedan solo locales,
# el export CSV más reciente SÍ se commitea (backup en la nube vía el repo)
respaldo/*
!respaldo/catalogo-export.csv

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
